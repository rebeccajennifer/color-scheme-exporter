# Description

This repository contains a python script for generating color scheme profiles for various terminal programs. For usage, show the help for `color_scheme_driver.py`:

```bash
python color_scheme_driver.py --help
```
